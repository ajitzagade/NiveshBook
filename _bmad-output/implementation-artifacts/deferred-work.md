- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-login-and-logout.md`
  summary: ~~Session TTL is an absolute 30-minute expiry, not renewed on activity.~~ **Resolved by Story 1.4** — `getSession()` now renews `expiresAt` on every successful resolution.
  evidence: n/a — kept for history only.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-login-and-logout.md`
  summary: ~~A user deactivated after logging in keeps their authenticated session until natural expiry.~~ **Resolved by Story 1.6** — `PATCH /api/users/[id]` (`setUserActiveStatus`) deletes every one of the target's `sessions` rows on a true→false transition, so `getSession()` still never re-checks `users.active`, but there's no live session left for it to resolve.
  evidence: n/a — kept for history only.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-login-and-logout.md`, `_bmad-output/implementation-artifacts/spec-1-4-session-timeout-owner-admin-revocation.md`, `_bmad-output/implementation-artifacts/spec-1-5-roles-the-server-side-authorization-gate.md`, `_bmad-output/implementation-artifacts/spec-1-6-owner-admin-activates-or-deactivates-a-user.md`, `_bmad-output/implementation-artifacts/spec-2-6-optional-partner-sub-partner-visibility-grant.md`
  summary: `packages/db/src/ports.ts` (the Drizzle-backed port implementations) has zero test coverage against a real database.
  evidence: only `packages/core/src/auth.ts` is unit-tested, against in-memory fake ports; every `apps/web` route test mocks `@niveshbook/db` away entirely. Story 1.4 added three more untested methods (`touchSession`, `listSessionsByUser`, `deleteSessionById`) on top of Story 1.1's three; Story 1.5 added two more (`findUserById`, `listAllUsers`); Story 1.6 added two more (`setUserActive`, `deleteAllSessionsForUser`) — ten methods now unverified against a real database, including `deleteSessionById`'s and `deleteAllSessionsForUser`'s security-relevant `userId` scoping. A correct DB-integration test needs a test-infra decision (e.g. testcontainers) beyond a trivial patch. Story 2.6's review (verification-gap) found the concrete shape this takes for `createPartnerShare`: the new `subPartnerVisibilityGrant` column has a DB-side default, making the field type-optional in Drizzle's insert type, so a future edit that silently drops `subPartnerVisibilityGrant: input.subPartnerVisibilityGrant` from the `.values({...})` call would compile clean and pass every test (all of which mock the port or use an in-memory fake) while every grant permanently persisted as `false` in production. Manually confirmed correct right now via live verification against the real dev Postgres, but nothing guards against this regressing later.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-login-and-logout.md`
  summary: Expired `sessions` rows are never purged from the database — unbounded table growth over time.
  evidence: Story 1.4's patch filters expired rows out of the *list view* (`listSessionsByUser`), but the rows themselves are never deleted. No cleanup job exists. Low urgency given the PRD's small-to-medium data volume NFR, but needs an eventual cron/scheduled-cleanup decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-login-and-logout.md`
  summary: `users.role` has no DB-level enum/check constraint — trusted from the app-level `UserRole` TypeScript union only.
  evidence: `packages/db/src/schema.ts` declares `role: text("role").notNull()` with no constraint. Worth tightening when Story 1.5 builds out full role-based enforcement.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-login-and-logout.md`
  summary: AD-9's dependency-direction boundary is true by inspection only — no dependency-cruiser (or equivalent) tooling is installed to enforce it.
  evidence: `epic-1-context.md` claimed it was "lint-enforced via dependency-cruiser"; no such config/dependency exists anywhere in the repo. Recommend wiring this during Story 1.5, the authorization-gate story, when the package boundaries start carrying real security weight.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-owner-admin-activates-or-deactivates-a-user.md`
  summary: No audit trail (who/when) for `PATCH /api/users/[id]` activating/deactivating a user.
  evidence: this is a security-sensitive admin action recording no actor/timestamp anywhere in the schema or code. Pre-existing pattern — no story has built audit logging yet; epics.md's nav list names a future "Audit History" area as a separate, later concern.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-owner-admin-activates-or-deactivates-a-user.md`
  summary: `setUserActiveStatus`'s `setUserActive` write and `deleteAllSessionsForUser` bulk-delete are two sequential, non-transactional operations; a login racing a deactivation PATCH could create a new session after the bulk-delete step, which `getSession()` would still accept (it never re-checks `users.active`).
  evidence: `packages/core/src/auth.ts`'s `setUserActiveStatus` awaits `deps.users.setUserActive` then, only on a true→false transition, `deps.sessions.deleteAllSessionsForUser` — no transaction wraps the two. This narrows (but doesn't fully close) the pre-existing "`getSession()` never checks `active`" gap tracked above: existing sessions at the moment of deactivation are now correctly wiped, but a session created by a concurrently in-flight login is not. Closing it needs either a cross-port DB transaction (an AD-9 port-composition question) or a live `active` check in `login()`/`getSession()` — both larger than a trivial patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-owner-admin-manages-role-level-permissions.md`
  summary: No audit trail (who/when) for granting/revoking Extra Withdrawal approval authority via `PATCH /api/permissions/[userId]`.
  evidence: a financially-sensitive permission (FR25/FR45) recording no actor/timestamp anywhere. Same pre-existing pattern as Story 1.6's deactivation-audit gap — no story has built audit logging yet.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-owner-admin-manages-role-level-permissions.md`
  summary: Generated migration `packages/db/drizzle/0002_misty_warpath.sql` (adds `can_approve_extra_withdrawal`) has not been applied to any live database.
  evidence: no project Postgres instance was reachable (docker/colima not running) during implementation or review. Run `pnpm --filter @niveshbook/db db:migrate` against every environment with real data before this ships.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-owner-admin-manages-role-level-permissions.md`
  summary: No guard against revoking the last remaining Extra Withdrawal approver, or an Owner/Admin revoking their own grant — could leave zero users able to approve once Epic 4 enforces it.
  evidence: `packages/core/src/permissions.ts`'s `setApprovalAuthority` performs no such check; re-confirmed with the human as an accepted risk during Story 1.7's review, consistent with Story 1.6's identical self-lockout precedent. No functional consequence today since Epic 4 (the only consumer) doesn't exist yet — revisit if/when it's built.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-per-client-configuration.md`
  summary: `client.config`'s `enabledModules.projectAdmin` only affects what `GET /api/permissions` displays — nothing in `authorize.ts`, `login()`, or user creation actually consults it, so a manually-seeded `project_admin` user can log in and act regardless of the config value.
  evidence: this is a pre-existing gap since Story 1.5 first introduced `project_admin` as a valid-but-ungated role value; Story 1.8's Approach explicitly scoped itself to the Permissions view and `layout.tsx` only. Revisit once a story actually needs to enforce this role's availability (e.g. a user-creation/role-assignment endpoint, or `authorize()` gaining a project_admin-scoped action).

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-create-and-edit-a-project.md`
  summary: Generated migration `packages/db/drizzle/0003_nifty_caretaker.sql` (adds the `projects` table) had not been applied to any live database at implementation time; since resolved for local dev.
  evidence: no Postgres instance was reachable (docker/colima not running) during implementation — same constraint noted for Story 1.7's migration. Applied and verified locally during step-03 review (colima + `docker compose up`, `db:migrate` + `db:seed`, full flow exercised against real Postgres — see spec-2-1's Implementation Notes). Still needs `pnpm --filter @niveshbook/db db:migrate` run against staging/production before this ships to any environment with real data.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-create-and-edit-a-project.md`
  summary: `GET`/`POST`/`PATCH /api/projects[/[id]]` are Owner/Admin-only for every role, including `GET` (list/view) — a Partner or Sub-partner gets 403 on the Projects screen entirely, even though `EXPERIENCE.md`'s IA table lists Projects as "Everyone (scoped)".
  evidence: `packages/core/src/authorize.ts`'s `PERMISSIONS` map grants all three `projects:*` actions to `owner_admin` only, per spec-2-1's Decisions ("no Project-shaped `ResourceRef` yet"). This is by design for this story — Partner/Sub-partner project-scoped visibility is Story 2.4+'s job once Partner Share records exist to scope against. `apps/web/lib/session-guard.ts`'s `requireOwnerAdminSession()` now gates the entire `(dashboard)` route group to `owner_admin` (redirecting any other authenticated role to `/`), so the sidebar's "Projects" nav item no longer renders for a role that can't use it — that stopgap holds until Story 2.4+ builds real per-role scoping and the shell can open back up to "everyone (scoped)" per `EXPERIENCE.md`'s IA table.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-create-and-edit-a-project.md`
  summary: No audit trail (who/when) for Project creation/edits via `POST`/`PATCH /api/projects`.
  evidence: same pre-existing pattern as Story 1.6/1.7's audit-trail gaps — no story has built audit logging yet; `epics.md`'s nav list names a future "Audit History" area as a separate, later concern.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-add-edit-partner-shares-with-100-validation.md`
  summary: No way to remove a mistakenly-added Partner Share — Owner/Admin can only overwrite via edit (which still versions it, per AD-3), never retract an accidental add.
  evidence: not required by epics.md's Story 2.2 AC, which only covers add/edit. Found during step-04 review (blind-hunter); flagged as a real gap since a fat-fingered add currently has no undo path. Implementing removal is a new feature, not a small patch — needs a design decision (soft-delete flag vs. a zero-percent "retracted" version vs. something else) that should weigh against AD-3's versioning philosophy before being built.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-sub-partner-allocation-as-of-full-project.md`
  summary: No test anywhere covers `apps/web/app/(dashboard)/projects/[id]/shares/page.tsx`'s dialog-mode routing (`handleSubmit`'s `add`/`edit`/`add-sub`/`edit-sub` branches) or the new pure helpers `subAllocationMessage`/`retainedMessage`, including the over-allocated (negative "retained") branch the spec's own Decisions explicitly call out as legitimate.
  evidence: found independently by both the verification-gap and blind-hunter review layers during step-04 review. Repo-wide search confirms no RTL/component test exists for this page, for this story or its Story 2.2 predecessor (same accepted, pre-existing gap pattern). Already named as a known, unresolved risk in spec-2-3's own Implementation Notes. A regression in which dialog mode calls which client-lib function, or a sign flip in the over/under-allocation branches, would ship undetected. Fix needs an RTL test driving `handleSubmit` per dialog mode plus direct unit tests of the two now-easily-extractable pure functions — beyond a trivial patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-co-partner-privacy-boundary.md`
  summary: No uniqueness constraint on `partner_shares.user_id`/`subpartner_shares.user_id` — the same `userId` could be linked (by an Owner/Admin action, deliberate or mistaken) to two different Partner Shares, on the same or different Projects, granting that one session `subpartner_shares:list` access to both Partners' internal structures simultaneously.
  evidence: found during step-04 review (blind-hunter); this is an explicit, checkpointed spec Decision ("no uniqueness enforcement... not required by any AC; enforcing it is scope creep for this story"), so it's recorded here rather than patched now. Requires trusted Owner/Admin action to create (not externally exploitable), but is the same *shape* of cross-partner leak this story exists to prevent. Revisit if Story 2.6's visibility-grant design (or an audit of linking mistakes) surfaces a real incident.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-sub-partner-own-data-visibility.md`, `_bmad-output/implementation-artifacts/spec-2-6-optional-partner-sub-partner-visibility-grant.md`, `_bmad-output/implementation-artifacts/spec-4-6-sub-partner-withdrawal-private-independent.md`
  summary: `partner.projectId !== projectId` and `subShare.partnerId !== partnerId` (the cross-scope resolution checks in `apps/web/app/api/projects/[id]/partner-shares/[partnerId]/subpartner-shares/[subPartnerId]/route.ts`, both `GET` and the pre-existing `PATCH`; and the identical `partner.projectId !== projectId` check in `apps/web/app/api/projects/[id]/partner-shares/[partnerId]/route.ts`'s new `GET`, Story 2.6) use case-sensitive JS string comparison, while the underlying Postgres `uuid` column lookup is case-insensitive — a caller who hand-types a differently-cased (but genuinely matching) UUID path segment could get a false 404/403 despite the id actually resolving.
  evidence: found during step-04 review (edge-case-hunter) on Story 2.5, and found again independently on Story 2.6's structurally identical new route. Real mechanism, verified: Postgres's `uuid` type normalizes on comparison so the DB lookup still finds the row, but the in-memory `!==` check compares Postgres's canonical-lowercase output against the caller's raw-cased URL string. Not introduced by either story — inherited unchanged from `PATCH`'s pre-existing code (Story 2.3), never flagged across Stories 2.2/2.3/2.4's reviews of the identical pattern. Narrow in practice (ids are always copy-pasted verbatim from a prior API response in every real client flow, never hand-typed with different casing), but a genuine correctness gap if ever hit, and now present in four routes (Story 4.6's `my-withdrawal-status/route.ts` added a fourth occurrence: target-share resolution via case-sensitive `shareId` match, while `authorize()`'s self-access `ownerId` match is case-insensitive). Fix (case-insensitive comparison, or `.toLowerCase()` normalization at read time) touches multiple routes across two epics — a deliberate sweep, not a one-route patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-carry-forward-recommended-available-withdrawal.md`
  summary: `packages/db/src/withdrawal-transaction-port.test.ts`'s `recordTransaction` idempotency test (live-Postgres) intermittently fails with a unique-constraint collision on `withdrawal_transactions_idempotency_key_unique` when the full `pnpm turbo run test` suite runs in parallel across packages, but passes cleanly every time in isolation (127/127).
  evidence: reproduced 3 times across Stories 4.3 and 4.4's own full-suite re-verification passes (never once in isolation). Not caused by either story's actual changes — Story 4.4's changes are web-only and don't touch this test or its table. Likely a test-isolation gap under `turbo`'s parallel package execution against the one shared local dev Postgres instance (no per-package/per-run DB namespacing). Worth a look next time a `packages/db` live-Postgres test is touched, or as its own small infra story — not a one-line patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-8-auto-linked-cross-project-movement.md`
  summary: `POST .../destination-allocations` re-validates the chosen destination requirement/share against current data unconditionally on every request, before it has any way to know whether the request is a first submission or a retry of an already-saved allocation — so a legitimate idempotent replay could get a spurious `404 not_found` instead of the correct 200 replay, if the destination requirement/share became stale between the original save and the retry.
  evidence: found during Story 4.8's edge-case-hunter review. Currently unreachable in practice: there's no way to delete an `investment_requirements` row, and Partner/Sub-partner Shares are append-only/stable-id, so "no longer resolves" has no live trigger today. Revisit if a future story adds requirement/share deletion — fix would need the route to check for an existing matching allocation (or defer requirement/share re-validation until after a replay check) before re-resolving against current data.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-7-post-withdrawal-destination-allocation.md`, `_bmad-output/implementation-artifacts/spec-4-9-available-balance-ledger.md`
  summary: `POST .../destination-allocations/route.ts` calls `projectPort.findProjectById(projectId)` (a DB read, line ~121) before `authorizeScope()` (line ~146) — an AD-1 ordering violation ("every route calls authorize()/authorizeScope() before touching data"). An unauthorized caller can distinguish a 404 (project doesn't exist) from a 403 (project exists, not authorized) without ever being authorized.
  evidence: found during Story 4.9's blind-hunter review, which noticed Story 4.9's own new `available-balance/spend/route.ts` had copied this exact ordering from this pre-existing route (fixed in 4.9's own patch round; this entry is only for the original, still-unfixed occurrence). Low severity — no data is exposed beyond project-existence, and both routes are Owner/Admin-only end to end regardless of ordering — but worth a small, low-risk fix (swap the two calls) next time this route is touched.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-9-available-balance-ledger.md`
  summary: New route tests (`available-balance/route.test.ts`, `available-balance/spend/route.test.ts`) verify authorization purely via role-fixture outcomes (partner session → 403, owner session → 200/201) rather than spying on `authorizeScope()`'s call arguments — so a copy-paste bug passing the wrong action string (e.g. reusing another action that happens to resolve to the identical `Set(["owner_admin"])` permission) would produce identical pass/fail results and go uncaught.
  evidence: found during Story 4.9's verification-gap review; explicitly noted as matching an existing pattern across every other route test in this codebase, not a regression introduced by this story. Fixing it well would mean auditing/strengthening this pattern codebase-wide (spy on `authorizeScope` and assert the exact action string per route), which is a sweep, not a one-route patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-10-end-to-end-money-trail.md`
  summary: `reconcileMoneyTrail()`'s tree walk (`packages/core/src/money-trail-reconciliation.ts`) uses a single `visited` `Set` shared across the entire traversal, unlike `assembleMoneyTrail()`'s deliberately path-scoped (per-branch) cycle protection. If a future story ever allowed lateral duplication (e.g. two `"available_balance"` legs on the same withdrawal converging on a shared downstream node, or any other case where two different branches of the trail legitimately reach the same real entity), a node visited via one branch could cause a discrepancy check to be silently skipped when reached again via a second, unrelated branch.
  evidence: found independently by both Story 4.10's blind-hunter and edge-case-hunter reviews. Confirmed NOT exploitable under the current schema/business rules — every leg/movement/investment-transaction in this domain is created by at most one write path, so no genuine "diamond" convergence of two branches onto the same fully-expanded node is possible today. Revisit (make `visited` path-scoped like `assembleMoneyTrail`'s, or add a comment documenting the invariant it relies on) only if a future story introduces any such lateral duplication.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-compute-investment-adjustment.md`
  summary: A Partner-with-Sub-partners' own adjustment row compares their own-only `actualPaid` against the *pooled* `shouldPay` (own + all current sub-partners), instead of a like-for-like comparison — can report "Pending ₹X" even when the family (partner + subs combined) has actually fully paid or overpaid.
  evidence: found and live-reproduced 2026-09-25 during a production seeding/scenario-validation exercise. Reproduction: Rajesh Mehta (40% share, 15% own-retained + Ananya 15% + Vikram 10%) — his family paid ₹12,00,000 (own) + ₹7,50,000 (Ananya) + ₹5,00,000 (Vikram) = ₹24,50,000 against a pooled Should Pay of ₹20,00,000 (genuinely ₹4,50,000 over-funded), but `GET .../adjustments` reported `{"adjustmentType":"pending","adjustmentAmount":"800000"}` — computed as pooled `shouldPay` (2,000,000) minus own-only `actualPaid` (1,200,000), an apples-to-oranges subtraction. Root cause: `packages/core/src/investment-adjustment.ts`'s `resolveAndUpsertAdjustment` (lines ~76-90) is called for the partner row at lines ~142-149 with `partner.shouldPay` (pooled) while its internal `actualPaid` sum (via `shareKey("partner", partnerId)`) only ever picks up that partner's own transactions, never their sub-partners'. This directly contradicts two other places in the same codebase: `_bmad-output/implementation-artifacts/epic-3-context.md`'s stated rule "Adjustments are per-person, not pooled — one person's carry-forward never affects another's Should Pay," and `apps/web/app/(dashboard)/projects/[id]/add-money/page.tsx`'s own comment (~lines 836-845) that a partner's pooled total and their own retained obligation "must never be conflated" for display purposes. The identical over-payment shape for a partner *without* sub-partners (verified same day: Priya Deshmukh in Aspire Buildcon, Rajesh Mehta in Aspire Indu) computes correctly as `extra_paid` — isolating the defect to the pooling path specifically. `packages/core/src/investment-adjustment.test.ts`'s only partner-with-subs test (lines ~178-216) doesn't assert on the combined family total either, so it would pass even with this defect present. Fix needs a design decision: either compare the partner's own row against `ownShouldPay` instead of pooled `shouldPay` (honoring "per-person"), or make the partner row's `actualPaid` itself pooled (own + subs) to match a pooled `shouldPay` — not a one-line patch either way.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-carry-forward-recommended-amount.md`
  summary: A new funding requirement's `previousPending`/`previousExtraPaid`/`recommendedAmount` carry-forward snapshot is copied from the prior requirement's `investment_adjustments` ledger at the exact moment the new requirement is created — but that ledger is only ever recomputed lazily, as a side effect of a human viewing `GET .../adjustments` or `GET .../my-investment-status` for the *prior* round. If a payment in the prior round is cancelled and correctly replaced without anyone viewing that round's Adjustments in between, the new round's snapshot is taken from the stale, pre-correction ledger state and is never revised afterward, even once the source ledger is later refreshed.
  evidence: found and live-reproduced 2026-09-25 during a production seeding/scenario-validation exercise. Reproduction: Vikram Joshi (Aspire Signature, 10% sub-partner share) cancelled his ₹5,00,000 Round 1 payment and immediately re-recorded a correct ₹5,00,000 replacement, all before Round 2 was created. Round 2's `GET .../should-pay` reports `"previousPending":"500000.00"` for him — as if he'd paid nothing in Round 1. Confirmed this is not a display lag: subsequently viewing Round 1's `GET .../adjustments` correctly shows him fully paid (`adjustmentType:"none"`), but Round 2's already-created snapshot does not update to reflect it. Root cause: `packages/core/src/recommended-amount.ts`'s `previousAmountsFor` (lines ~53-66) reads whatever is currently persisted in `investment_adjustments` at snapshot time (`snapshotRecommendedAmounts`, called from `POST .../investment-requirements`, `apps/web/app/api/projects/[id]/investment-requirements/route.ts` lines ~227-244) with no forced recompute first; the recompute only happens inside `GET .../adjustments`/`GET .../my-investment-status`'s own handlers. Impact: overstates what a partner is asked to bring to the next round by the exact amount they already settled via a cancel/replace in a prior round — a direct partner-facing financial-accuracy issue. Fix needs the new-requirement creation path to force a fresh recompute of every current share's adjustment (not just read the possibly-stale row) before taking the carry-forward snapshot.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-money-history-list.md`
  summary: `available_balances` pool mutations (both credit via an allocation leg and debit via a spend, or a debit-back via Story 4.11's cancel cascade) have no append-only audit trail — only the running `balance` column is mutated in place. Money History has no read path onto `available_balances` at all (only the separate `available_balance_spends` table), so when a withdrawal's `"available_balance"` leg gets cancelled and `debitBalance()` reverses the pool credit, the original "Added to Available Balance" Money History entry stays displayed forever as if still live, with nothing anywhere explaining the pool was actually zeroed back out.
  evidence: found during Story 5.1's edge-case-hunter review, while tracing the interaction between Story 4.11's cancel cascade and this story's read model. A partial mitigation shipped in Story 5.1's own patch round (the leg's Money History entry is now marked "reversed" via its parent withdrawal's status, giving at least some signal) — but the deeper gap (no visibility into the pool's own balance history, e.g. "credited ₹50,000 on date X, debited back ₹50,000 on date Y due to a cancel") remains unaddressed. Fixing this well would need a new append-only ledger table for `available_balances` mutations (credit/debit/reason/linked-transaction), which is real new schema/scope — not a one-route patch. Revisit alongside Story 5.9 (Audit History Viewing Surface) if that story's own scope doesn't already cover it, or as its own small story.
