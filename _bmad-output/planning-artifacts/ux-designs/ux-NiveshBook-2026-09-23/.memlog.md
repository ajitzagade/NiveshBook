---
topic: NiveshBook UX
updated: 2026-09-23T21:27
---

- (assumption) No concrete visual reference (Figma/image) exists anywhere -- PRD Sec.6 explicitly flags this as unresolved Open Question 1. Proceeding fast-path from the prose direction in epics.md + PRD Sec.6 + architecture spine, tagged [ASSUMPTION] throughout.
- (assumption) Styling foundation: shadcn/ui on Tailwind CSS, chosen since apps/web has zero styling deps today and this is the standard accessible-primitive pairing for Next.js 16 + React 19; gives WCAG-sane defaults for free which a real-money app needs.
- (assumption) Stakes: consumer-grade trust polish despite small/internal usage -- this handles partners' real money and NFR16 explicitly forbids a generic/AI-templated look, so treating it as a trust-critical financial product, not an internal ops tool.
- (decision) User opted to skip further elicitation and proceed fast-path: draft both spines now with assumptions tagged, skip creative tools, skip reviewer gate.
- (event) DESIGN.md and EXPERIENCE.md drafted fast-path from PRD/epics/architecture; left status: draft (not final) -- several [ASSUMPTION] tags remain (visual reference, money-trail interaction, stale-edit UX, md-breakpoint sidebar behavior, brand color AA contrast check) pending founder confirmation. Reviewer gate and doc-standards polish pass skipped per user's explicit speed preference.
- (change) Real mockup provided (claude.ai artifact 89658c4b, saved to imports/founder-mockup.html) -- reconciling DESIGN.md/EXPERIENCE.md against it as ground truth, replacing earlier prose-only assumptions. Corrections: primary #2F5FD1->#2F6FED, success #1B8F5A->#17A566, danger #D0413F->#E5484D, teal #0E8C86->#0EA5A5, violet #7C5CD6->#8B5CF6; added 5th semantic color amber #E8A317 (not previously specified); typography is system font stack, not Plus Jakarta Sans (that assumption is dropped); radius is card:14px/el:9px, not the sm/md/lg scale guessed earlier; trail is a vertical timeline with colored dot markers, not horizontal; nav badges are mostly neutral slate with only 5 of 9 items semantically colored, not uniformly colored as assumed; added trace-banner and 5-variant status chip (incl. neutral) not previously modeled. Mockup itself uses hand-rolled CSS custom properties, not Tailwind/shadcn -- decision: keep shadcn/ui primitives (Dialog/Popover/Sheet/Combobox) for interactions the static mockup doesn't demonstrate, but source all tokens (color/radius/shadow/type) from this mockup, not shadcn defaults.
- (event) Reusable component library built in packages/ui per reconciled DESIGN.md: Button, Card, StatusChip (5 variants), Amount (Indian-grouped), StatCard, WalletHero, Table+sub-rows, ShareRow/DistributedCheck, SplitRow, AdjustPersonCard, Trail/TraceBanner, ReportTile, NavItem, Helper, Dialog/Popover/DropdownMenu (Radix). Tailwind v4 wired with tokens sourced from imports/founder-mockup.html. Verified: repo-wide lint/typecheck/build/test/audit/dependency-boundaries all clean; rendered smoke test confirmed correct Indian currency formatting, chip variants, active nav state -- caught and fixed one real bug (StatCard defaulting non-money counts to rupee formatting) via that render, not just types.
- (decision) User asked to expand the founder-mockup artifact (claude.ai artifact 89658c4b, same as imports/founder-mockup.html) into a fuller demo covering every screen. Scope decisions: (1) include all 12 IA surfaces, not just the founder's original 11 -- adding Users, Permissions, Audit History as new Owner/Admin-only screens (role-gated in the mockup's existing role-switcher, matching NFR18/FR7/FR8's never-shown-when-unauthorized rule). (2) Reference image (dark-theme horizontal branching flow-chart of a money trail) is layout inspiration only, not a palette change -- adds a horizontal branching 'Trail Quick View' as an alternate expanded view of the same FLOW-A-1024 trail data, wired to the mockup's existing (previously inert) 'Trail Quick View' button on Money History; the compact vertical timeline stays the default/primary pattern.
- (event) Rebuilding imports/founder-mockup.html-equivalent as an updated, more detailed all-screen demo artifact (same file structure/tokens, all screens polished, 3 new screens added, branching trail view added) and republishing to the same artifact URL so the user's existing demo link updates in place.
- (event) All-screen demo built at mockups/all-screens-demo.html (14 screens: original 11 plus Users/Permissions/Audit History, role-gated in the demo's role-switcher; Trail Quick View horizontal-branching toggle added to Money History) and published to the founder's existing artifact URL (89658c4b) so the shared link updates in place. DESIGN.md/EXPERIENCE.md updated with the new Trail Quick View component/pattern and Withdraw Money / Partner Shares worked-example helper text (previously missing despite being required by the existing Component Patterns table). DESIGN.md/EXPERIENCE.md status left as draft -- this was a demo-mockup update, not a full Finalize pass (reviewer gate not run, per no explicit ask).
- (change) Renamed all demo placeholder people to real names: Partner A -> Sandip Z, sub-partners -> Mahesh Z / Harischandra A / (new 3rd sub-partner) Chandrakant Z, Partner B -> Yogesh Shilimkar, Partner C -> ABC Group. Rebalanced Sandip Z's 50% share into 20/10/10/10 across himself + 3 sub-partners (was 25/12.5/12.5 across himself + 2). Rebuilt Trail Quick View from a single 4-generation branch into a multi-lane 8-generation trace (shared funding+withdrawal trunk, then 3 lanes: Project B re-withdrawal+re-split+re-reinvest, Person X re-investment, wallet later-reinvestment+Extra Withdrawal+sub-partner Mahesh Z's own downstream reinvestment) covering every destination type in the product. Republished to the same artifact URL (89658c4b).
- (change) User reported the demo felt dead -- forms didn't open and buttons did nothing. Wired every remaining button in the mockup: modal+toast infrastructure added; New Transaction now opens an Add Money/Withdraw Money chooser; Add Partner, Use Balance, Invite User, and Manage User open real forms; Save Funding/Save and Complete/Save Project navigate onward with a confirmation toast; Adjust Next Time's two filter buttons actually filter; Money History gained a working All Transactions list view and Export dropdown alongside the existing trail toggle; report tiles, Deactivate/Reactivate, Grant/Revoke, and Enable/Disable all give live feedback; Project rows and share-row Edit chips are clickable. Republished to the same artifact URL (89658c4b).
- (change) User flagged the Trail Quick View's generic column-to-column arrow as misleading (arrow appeared centered/toward the whole next column, not toward the actual merge target -- e.g. it should point specifically at Sandip Z's total, not float between his total and Yogesh/ABC). Restructured the trail from 3 separate lane-rows into a single 8-generation-column layout (same content, all 23 nodes) and replaced the flat glyph arrows with a real per-edge SVG connector system: 22 explicit fromNode->toNode arrows (colored by transaction type, arrowhead per edge), computed via getBoundingClientRect against a scrollable canvas and redrawn whenever Trail Quick View becomes visible or the window resizes. Now every merge (4 contributions -> Sandip Z's total, 3 partner totals -> Project A funded) and every split (Withdrawal -> 3 destinations, Wallet -> 2, Withdrawal-from-Project-B -> 2) is an unambiguous individual line, not a shared column-level arrow. Republished to the same artifact URL (89658c4b).
- (change) User asked whether entering a project name would scope the demo to that project's own data, and asked for a single-partner (Sandip Z + sub-partners only) view instead of always seeing every partner -- also flagged that different projects may have different/overlapping partner rosters. Verified against ARCHITECTURE-SPINE.md: PARTNER_SHARE is keyed by (shareId, projectId), so this is already the intended data model, just not previously demoed. Added: (1) a project search/switcher in the sidebar (type-ahead, datalist) backed by a real second example -- Project B, with Yogesh Shilimkar + ABC Group carried over at different shares/amounts plus a brand-new partner Kavita Joshi, and Sandip Z NOT a partner there at all; (2) Partner Shares/Add Money/Withdraw Money rewritten to render from this per-project data model instead of static HTML, respecting the existing role switcher (switching to 'Sandip Z' or 'Mahesh Z' while on Project B now correctly shows a 'You don't have access to this' empty state, since neither is a partner there); (3) a new Partner Detail screen, reached by clicking any partner's name, scoped to just that partner + their sub-partners' shares and Add/Withdraw Money history. Documented both new patterns (project search, partner detail) in EXPERIENCE.md's Component Patterns and the Partner Shares IA row, citing the architecture ERD. Also fixed a bug from an earlier rename pass: global replace of 'Partner C' -> 'ABC Group' had corrupted 'Partner Contributions' into 'ABC Groupontributions' on the Add Money screen. Republished to the same artifact URL (89658c4b).
