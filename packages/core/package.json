{
  "name": "@niveshbook/core",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@niveshbook/types": "workspace:*",
    "argon2": "^0.45.1",
    "uuidv7": "^1.2.1"
  },
  "devDependencies": {
    "@niveshbook/config": "workspace:*",
    "@types/node": "^22.7.5",
    "eslint": "^9.39.5",
    "typescript": "^5.6.3",
    "vitest": "^5.0.1"
  }
}
